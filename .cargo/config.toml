# ahash (via asciidoc-parser) pulls getrandom, which needs an explicit backend
# on wasm32-unknown-unknown. The `wasm_js` feature alone is insufficient.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
